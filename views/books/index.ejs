<h2 class="page-header" >Search Books</h2>
<form action="/books" method="GET">
<div>
    <label>Title</label>
    <input type="text" name="title" value="<%= searchOptions.title %>">
</div>
<br>
<div>
    <label>Published After</label>
    <input type="text" name="publishedAfter" value="<%= searchOptions.publishedAfter %>">
</div>
<br>
<div>
    <label>Published Before</label>
    <input type="text" name="publishedBefore" value="<%= searchOptions.publishedBefore %>">
</div>

<button type="submit">Search</button>

</form>


<div>
<% books.forEach(book => { %>
    <a href="/books/<%= book.id %>">
        <img height="350" width="300" src="<%= book.coverImagePath %>">
    </a>
<% })%>
</div>